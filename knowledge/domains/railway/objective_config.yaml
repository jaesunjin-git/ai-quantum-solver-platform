# ============================================================
# Railway 도메인 Objective Builder 가중치 Override
# ============================================================
# configs/objective_defaults.yaml의 범용 값을 도메인에 맞게 override.
#
# 우선순위: params(사용자) > 이 파일 > objective_defaults.yaml > dataclass 기본값
# ============================================================

# 현재: 기본값 사용 (override 없음)
# block-gap-block 튜닝 시 여기에 값을 추가합니다.
#
# 예시 (Phase A 튜닝):
# idle_penalty_weight: 0.005     # 0.01 → 0.005 (gap 벌점 50% 감소)
